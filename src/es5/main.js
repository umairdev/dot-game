"use strict";

var main = {
  init: function init() {
    // initialize the application js here
    console.log("App is initialized"); // initialize the canvas

    window.gameCanvas = new GameCanvas('game-container');
  }
};